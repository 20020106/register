<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.Register" parent="Theme.Material3.DayNight.NoActionBar">

        <item name="colorPrimary">@color/status</item>
        <item name="colorPrimaryDark">@color/status</item>
        <item name="colorAccent">@color/status</item>
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Register" parent="Base.Theme.Register" />
</resources>